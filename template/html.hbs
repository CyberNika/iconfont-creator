<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <title>Iconfont</title>
    <link rel="stylesheet" href="{{style}}">
    <style>
      body {
        padding: 20px 40px;
        margin: 0;
        color: #555;
      }
      h1 {
        padding-bottom: 12px;
        border-bottom: 1px solid #ccc;
      }
      p {
        margin: 8px 0;
      }
      a {
        color: #999;
      }
      a:hover {
        color: #666;
      }
      pre {
        padding: 15px;
        border-radius: 6px;
        background: #f7f7f7;
        white-space: pre-line;
        font-size: 14px;
        color: #666;
      }
      pre code {
        margin: 0;
        border: none;
        background: #f7f7f7;
        overflow-x: auto;
        font-family: monaco,menlo,consolas,courier new,courier,monospace;
      }
      main {
        margin-top: 20px;
      }
      footer {
        margin-top: 80px;
        text-align: center;
        color: #999;
      }
      .icon-list {
        padding-left: 0;
        margin-left: -8px;
        list-style: none;
      }
      .icon-item {
        display: inline-block;
        margin: 8px;
        width: 100px;
        text-align: center;
      }
      .icon {
        display: block;
        font-size: 42px;
        transition: transform 0.5s ease;
        cursor: pointer;
      }
      .icon:hover {
        /*font-size: 50px;*/
        transform: scale(1.5);
      }
      .name {
        margin-top: 14px;
        font-size: 12px;
      }
      .code {
        font-size: 12px;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Iconfont</h1>
    </header>

    <main>
      <section class="icon-section">
        <ul class="icon-list">
          {{#each icons}}
          <li class="icon-item">
            <i class="iconfont icon-{{name}} icon"></i>
            <p class="name">{{name}}</p>
            <p class="code">&amp;#x{{hexCode}};</p>
          </li>
          {{/each}}
        </ul>
      </section>

      <section class="help-section">
        <h1>使用帮助</h1>
        <h3>第一步：将 CSS 添加至 HTML 中</h3>
        <pre>
          <code>&lt;link rel="stylesheet" href="./iconfont.css"&gt;</code>
        </pre>
        <h3>第二步：查找相应图标的字体编码或 class，应用于页面</h3>

        <h4>unicode 引用</h4>
        <pre>
          <code>&lt;i class="iconfont"&gt;&amp;#xf001;&lt;/i&gt;</code>
        </pre>

        <h4>class 引用</h4>
        <pre>
          <code>&lt;i class="iconfont icon-all-fill"&gt;&lt;/i&gt;</code>
        </pre>
      </section>
    </main>

    <footer>
      <p>
        <a href="https://github.com/flyow/iconfont-creator" target="_blank">created by iconfont-creator</a>
      </p>
    </footer>
  </body>
</html>
